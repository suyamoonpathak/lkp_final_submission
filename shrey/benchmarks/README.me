# Make both files executable with the following commands
chmod +x run_wal_analysis.sh
chmod +x run_repeated.sh

# To run the analysis once use the following command
sudo bash ./run_wal_analysis.sh

# To run the analysis N times and tabulate the average of the results, run the following command
sudo bash ./run_repeated.sh N


